/*
ビット全探索で解く。
探索を適当に配列にいれてソート。

しなくていい。5以上のものはないからそこまでで比較。
指定された番号のものを返す。

ｋ以下のものを列挙して、辞書順ソートすればOK.

*/

#include <stdio.h>

int main(){
  char s[5000];
  int len, i, j;
  int t, sum, res = 0;

  char [5][]
  scanf("%s", s);

  len = strlen(s);


    res += sum;
  }
