「++ａ」と「a++」の違い
++a 処理の前に値を１プラスする。値が代入されるのはインクリメントされたあと
ex.
  b=++a     ａの値が１インクリメントされ、２になる。その後,ａがｂに代入される
　data[++ａ］  先にａがインクリメントされてから配列を参照する

a++ 処理を実行してからインクリメントを行う。
ex.
  b=a++  aの値をｂに代入したあと、インクリメントを行う。
  data[ａ++]  配列をaで参照したあと、インクリメントを行う。
